<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ddys.gemsell.mapper.OrderMapper">
        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.ddys.gemsell.entity.Order">
                    <id column="order_id" property="orderId"/>
                    <result column="user_id" property="userId"/>
                    <result column="user_name" property="userName"/>
                    <result column="good_id" property="goodId"/>
                    <result column="good_name" property="goodName"/>
                    <result column="price" property="price"/>
                    <result column="status" property="status"/>
                    <result column="create_time" property="createTime"/>
                    <result column="success_time" property="successTime"/>
                    <result column="number" property="number"/>
                    <result column="total_price" property="totalPrice"/>
        </resultMap>

    <sql id="selectVo">
        select
        order_id, user_id, user_name, good_id, good_name, price, status, create_time, success_time,number,total_price
       from order a
    </sql>
    <select id="getListByCondition" parameterType="com.ddys.gemsell.entity.Order" resultMap="BaseResultMap">
        <include refid="selectVo"></include>
         where 1=1
    </select>

</mapper>
